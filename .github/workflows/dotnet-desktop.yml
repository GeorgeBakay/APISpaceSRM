name: ASP.NET Web API CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:

    runs-on: windows-latest

    env:
      Solution_Name: 'APISpaceSRM.sln'  # Вкажіть ваше ім'я файлу рішення

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'  # Вкажіть версію .NET 6

    # Відновлення залежностей
    - name: Restore dependencies
      run: dotnet restore SpaceSRM.sln

    # Збірка проекту
    - name: Build
      run: dotnet build SpaceSRM.sln --configuration Release --no-restore

    